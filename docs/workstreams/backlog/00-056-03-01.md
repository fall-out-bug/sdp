---
ws_id: "00-056-03"
feature_id: "F056"
title: "Remaining Skills Instrumentation"
status: "backlog"
priority: "P1"
depends_on: ["00-054-05"]
blocks: ["00-056-04"]
project_id: "00"
---

# 00-056-03: Remaining Skills Instrumentation

## Goal

### 🎯 Goal

Instrument all remaining skills: `@vision`, `@reality`, `@oneshot`, `@prototype`, `@hotfix`, `@bugfix`, `@issue`, `@debug`.

## Context

These skills are lower-priority but needed for complete pipeline coverage. Most call existing CLI commands — just add evidence emit at the right points.

## Acceptance Criteria

- [x] AC1: `@vision` emits `plan` event (via `sdp skill record --skill vision --type plan`)
- [x] AC2: `@reality` emits `verification` event (via `sdp skill record --skill reality --type verification`)
- [x] AC3: `@oneshot` emits `plan` + `approval` (orchestrate.go: plan at start, approval on success)
- [x] AC4: `@prototype` emits `generation` event (prototype.go: after workstream generation)
- [x] AC5: `@hotfix` emits `generation` + `approval` (via `sdp skill record` twice)
- [x] AC6: `@bugfix` emits `generation` + `verification` (via `sdp skill record`)
- [x] AC7: `@issue` emits `plan` event (via `sdp skill record --skill issue --type plan`)
- [x] AC8: `@debug` emits `verification` event (via `sdp skill record --skill debug --type verification`)
- [x] AC9: All events non-blocking — Emit() or _ = EmitSync(); skill record never fails on evidence error

## Scope Files

**Implementation:**
- sdp-plugin/internal/evidence/emitter.go (update — add skill-specific helpers)
- sdp-plugin/cmd/sdp/prototype.go (update)
- sdp-plugin/cmd/sdp/orchestrate.go (update — oneshot)
- sdp-plugin/internal/evidence/emitter_test.go (update)

**Tests:**
- sdp-plugin/internal/evidence/emitter_test.go

## Notes

- ~2h work
- Batch: these are lower-priority skills — thin evidence (event type + timestamp + ws_id)
- Some skills have no CLI command — may need stub commands or skip evidence for pure Claude skills

---

### Execution Report

- **Completed:** 2026-02-10
- **Artifacts:** evidence.SkillEvent(skillName, eventType, wsID, data); `sdp skill record --skill X --type Y`; prototype.go emits generation after workstream gen; orchestrate.go emits plan at start and approval on success; emitter_test.go TestSkillEvent.
- **Verification:** go build and go test ./internal/evidence pass.
- **Post-build:** Go build and test in sdp-plugin.
